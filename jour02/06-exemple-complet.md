# exemple de programme avec tout


```
FONCTION
    generer_text_etudiant( matiere )
        matieres = etudiants.matieres

        BOUCLE i 0 .. 1 AUGMENTER + 1
            SI matieres[i] == matiere 
                ECRIRE etudiant.nom + " a suivi le cours de " + etudiant.matieres[i] + " durant " + etudiant.duree + " heures"
START
    etudiant = {
        nom : "Alain"
        matieres : ["PHP", "JS"]
        duree : 6
    }
    # si etudiant a suivi un cours de PHP => écrire "Alain a suivi le cours de PHP durant 6 heures"
    generer_text_etudiant( "PHP" )
    generer_text_etudiant( "Css" )
    

    matieres = etudiants.matieres

    BOUCLE i 0 .. 1 AUGMENTER + 1
        SI matieres[i] == "PHP"
            ECRIRE etudiant.nom + " a suivi le cours de " + etudiant.matieres[i] + " durant " + etudiant.duree + " heures"
    
    # si etudiant a suivi un cours de Css => écrire "Alain a suivi le cours de CSS durant 6 heures"

     matieres = etudiants.matieres

    BOUCLE i 0 .. 1 AUGMENTER + 1
        SI matieres[i] == "Css"
            ECRIRE etudiant.nom + " a suivi le cours de " + etudiant.matieres[i] + " durant " + etudiant.duree + " heures"



    écrire la phase suivante :

    
    
    # écrire une phrase à partir de notre variable => concaténation 

    # récupérer l'ensemble des matières 
    matieres = etudiants.matieres

    # condition 
    BOUCLE i 0 .. 1 AUGMENTER + 1
        SI matieres[i] == "PHP"
            ECRIRE etudiant.nom + " a suivi le cours de " + etudiant.matieres[i] + " durant " + etudiant.duree + " heures"

    # vérifier est ce que cette liste contient la valeur PHP 
    # si c'est vrai alors je vais faire ma concaténation 
    SI 



    


END
```

